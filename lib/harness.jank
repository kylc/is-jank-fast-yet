(ns harness
  (:require [jank.perf :as perf]))

(def bench-opts {:epochs 100})

(defmacro bench [expr]
  `(let [result# (perf/bench-to-data bench-opts ~expr)
         mean#   (:mean result#)]
     (println "Elapsed time:" mean# "s")))
